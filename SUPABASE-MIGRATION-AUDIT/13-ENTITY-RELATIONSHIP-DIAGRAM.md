# 13 — Entity Relationship Diagram

## Current Data Relationships

```mermaid
erDiagram
    WORKSPACE ||--o{ PROFILES : has
    WORKSPACE ||--|| WORKSPACE_SETTINGS : configures
    PROFILES ||--o{ CONTACTS : manages
    PROFILES }|--|? PROFILES : reports_to
    PROFILES ||--o{ ACTIVITIES : performs
    PROFILES ||--o{ GIFTS : dispatches
    ORGANIZATIONS ||--o{ CONTACTS : contains
    CONTACTS ||--o{ ACTIVITIES : receives
    CONTACTS ||--o{ GIFTS : receives
    PROFILES ||--o{ CALENDAR_EVENTS : schedules
    PROFILES ||--o{ NUDGES : receives

    WORKSPACE {
        uuid id PK
        text name
        text subdomain
        text logo_url
        jsonb theme
        timestamp created_at
    }

    PROFILES {
        uuid id PK
        text email UK
        text name
        text role
        uuid manager_id FK
        uuid workspace_id FK
        text avatar_url
        text status
        timestamp created_at
    }

    ORGANIZATIONS {
        uuid id PK
        text name UK
        text sector
        text category
        text street_address
        text city
        text province
        text postal_code
        text phone
        text email
        uuid workspace_id FK
        timestamp created_at
    }

    CONTACTS {
        uuid id PK
        uuid org_id FK
        text name
        text email
        text phone
        uuid assigned_rep_id FK
        text status
        integer relationship_health
        text ai_recommendation
        uuid workspace_id FK
        timestamp created_at
    }

    ACTIVITIES {
        uuid id PK
        uuid contact_id FK
        uuid rep_id FK
        text type
        text grade
        text notes
        uuid workspace_id FK
        timestamp logged_at
    }

    GIFTS {
        uuid id PK
        uuid contact_id FK
        uuid rep_id FK
        text confection_type
        text category
        numeric amount
        text status
        text sender_label
        text reason
        uuid workspace_id FK
        timestamp dispatched_at
    }

    CALENDAR_EVENTS {
        uuid id PK
        uuid profile_id FK
        text type
        text target
        date event_date
        time event_time
        text agenda
        uuid workspace_id FK
        timestamp created_at
    }

    NUDGES {
        uuid id PK
        uuid profile_id FK
        text message
        text severity
        uuid workspace_id FK
        timestamp created_at
    }

    WORKSPACE_SETTINGS {
        uuid id PK
        uuid workspace_id FK
        integer decay_warning
        integer decay_critical
        integer target_conversion
        time hours_start
        time hours_end
        integer nudge_cap
        boolean auto_neglect
        boolean manager_override
        boolean alert_routing
        numeric budget_milestone
        numeric budget_monthly
        boolean approval_gate
        numeric approval_threshold
        boolean auto_gifting
        text webhook_slack
        text webhook_teams
        jsonb integrations
        timestamp updated_at
    }
```

## Mapping from Mock Data to Tables

| Mock Data Source | Target Table(s) |
|:-----------------|:----------------|
| `users.json` | `profiles` (via Supabase Auth) |
| `employeesData` | `profiles` |
| `customersData` / `prospectsData` | `organizations` + `contacts` |
| `clientGraphData` / touchpoint arrays | `activities` |
| `giftingDispatchSchedule` / `activeB2BOrders` | `gifts` |
| `calendarEventsDB` | Derived view from `gifts` (not a separate table) |
| `executiveCalendarEvents` | `calendar_events` |
| `executiveNudges` | `nudges` |
| `executiveActivityHistory` / `liveActivityFeed` | `activities` (filtered by role) |
| `leaderboardData` / `employeesLeaderboardDataSets` | Computed views from `activities` + `gifts` |
| `activeAuditsDB` (COSMO) | Generated by AI engine from `activities` + `contacts` |
| `window.rmosSettings` | `workspace_settings` |
